spring:
  profiles:
    active:
      - local_dev
  messages:
    basename: message
    encoding: UTF-8
    fallback-to-system-locale=: true
logging:
  level:
    org:
      springframework: info
server:
  port: 8080
application:
  face:
    env: "build"
  core:
    url: "localhost:8000"